name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - 'docs/**'
  pull_request:
    branches: [main]
    paths-ignore:
      - '**.md'
      - 'docs/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 20.x
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint --workspace=frontend

      - name: Audit dependencies
        run: |
          for attempt in 1 2 3; do
            npm audit --audit-level=high --workspace=frontend && exit 0
            echo "npm audit attempt $attempt failed (registry may be flaking), retrying in 15s..."
            sleep 15
          done
          exit 1

      - name: Build (includes tsc type-check)
        run: npm run build --workspace=frontend

  backend:
    name: Backend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: astral-sh/setup-uv@v9.0.0
        with:
          python-version: '3.14'
          working-directory: backend

      - name: Check lockfile is up to date
        run: uv lock --check
        working-directory: backend

      - name: Install dependencies
        run: uv sync --group dev
        working-directory: backend

      - name: Lint
        run: uvx ruff check --select=E9,F63,F7,F82 backend/

      - name: Test
        run: uv run python -m pytest
        working-directory: backend
